name: Build & Release Chopcord

on:
  workflow_dispatch:
    inputs:
      version:
        description: "Version to build and release"
        required: true
        default: "1.0.1"

permissions:
  contents: write

defaults:
  run:
    shell: bash

# --------------------------------------------------
# VALIDATION
# --------------------------------------------------
jobs:
  validate:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Cache pip
        uses: actions/cache@v4
        with:
          path: ~/.cache/pip
          key: ${{ runner.os }}-pip-${{ hashFiles('requirements.txt') }}

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt flake8

      - name: Lint
        run: flake8 .

      - name: Verify web assets
        run: |
          ls -la web
          test -f web/loading.html
          test -f web/offline.html
          test -f web/injector.js
          test -f web/update_modal.js

# --------------------------------------------------
# WINDOWS x64
# --------------------------------------------------
  build-windows:
    runs-on: windows-latest
    needs: validate

    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - uses: ./.github/actions/setup-build

      - name: Build Windows x64
        shell: pwsh
        run: |
          & .\.venv\Scripts\activate
          $version = "${{ github.event.inputs.version }}"

          python -m nuitka chopcord.py `
            --standalone `
            --onefile `
            --disable-console `
            --include-data-dir=web=web `
            --include-data-files=logo.png=logo.png `
            --include-data-files=logo.ico=logo.ico `
            --windows-icon-from-ico=logo.ico `
            --windows-product-name="Chopcord" `
            --windows-file-version="$version" `
            --output-dir=dist `
            --output-filename="chopcord-windows-x64-v$version.exe" `
            --assume-yes-for-downloads

      - name: Zip
        shell: pwsh
        run: |
          Compress-Archive `
            dist\chopcord-windows-x64-v${{ github.event.inputs.version }}.exe `
            dist\chopcord-windows-x64-v${{ github.event.inputs.version }}.zip

      - uses: actions/upload-artifact@v4
        with:
          name: chopcord-windows-x64
          path: dist/chopcord-windows-x64-v${{ github.event.inputs.version }}.zip

# --------------------------------------------------
# LINUX x64
# --------------------------------------------------
  build-linux:
    runs-on: ubuntu-22.04
    needs: validate

    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - uses: ./.github/actions/setup-build

      - name: Install system deps (pywebview)
        run: |
          sudo apt update
          sudo apt install -y \
            libgtk-3-0 \
            libwebkit2gtk-4.0-37 \
            libnss3 \
            libxss1 \
            libasound2

      - name: Build Linux x64
        run: |
          source .venv/bin/activate
          VERSION="${{ github.event.inputs.version }}"

          python -m nuitka chopcord.py \
            --standalone \
            --onefile \
            --disable-console \
            --include-data-dir=web=web \
            --include-data-files=logo.png=logo.png \
            --linux-onefile-icon=logo.png \
            --output-dir=dist \
            --output-filename="chopcord-linux-x64-v$VERSION" \
            --assume-yes-for-downloads

      - name: TAR
        run: |
          tar -czvf chopcord-linux-x64-v${{ github.event.inputs.version }}.tar.gz \
            -C dist chopcord-linux-x64-v${{ github.event.inputs.version }}

      - uses: actions/upload-artifact@v4
        with:
          name: chopcord-linux-x64
          path: chopcord-linux-x64-v${{ github.event.inputs.version }}.tar.gz

# --------------------------------------------------
# macOS ARM64
# --------------------------------------------------
  build-macos-arm64:
    runs-on: macos-latest
    needs: validate

    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - uses: ./.github/actions/setup-build

      - name: Build macOS ARM64
        run: |
          source .venv/bin/activate
          export DYLD_LIBRARY_PATH=""
          VERSION="${{ github.event.inputs.version }}"

          python -m nuitka chopcord.py \
            --standalone \
            --macos-create-app-bundle \
            --macos-sign-identity="-" \
            --macos-app-name="Chopcord" \
            --macos-app-icon=logo.icns \
            --macos-app-version="$VERSION" \
            --include-data-dir=web=web \
            --include-data-files=logo.png=logo.png \
            --assume-yes-for-downloads \
            --output-dir=build-arm64

      - name: Rename App
        run: |
          APP=$(find build-arm64 -name "*.app" | head -n1)
          mv "$APP" "Chopcord-arm64-v${{ github.event.inputs.version }}.app"

      - name: Create DMG
        run: |
          hdiutil create \
            -volname "Chopcord ARM64" \
            -srcfolder "Chopcord-arm64-v${{ github.event.inputs.version }}.app" \
            -format UDZO \
            -ov \
            -o "chopcord-macos-arm64-v${{ github.event.inputs.version }}.dmg"

      - uses: actions/upload-artifact@v4
        with:
          name: chopcord-macos-arm64
          path: chopcord-macos-arm64-v${{ github.event.inputs.version }}.dmg

# -------------------------
# RELEASE
# -------------------------
  release:
    name: Create GitHub Release
    runs-on: ubuntu-22.04
    needs:
      - build-windows
      - build-linux
      - build-macos-arm64
      - build-macos-intel

    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - uses: actions/download-artifact@v4
        with:
          path: artifacts

      - name: Extract Changelog Section
        run: |
          VERSION="v${{ github.event.inputs.version }}"
          awk -v ver="$VERSION" '
            $0 ~ "^## " ver {flag=1; next}
            flag && /^## / {exit}
            flag {print}
          ' CHANGELOG.md > RELEASE_NOTES.md

      - name: Generate Checksums + Download Table
        shell: bash
        run: |
          REPO="${{ github.repository }}"
          VERSION="${{ github.event.inputs.version }}"
          TAG="v$VERSION"

          WIN_X64=$(find artifacts -name "*windows-x64*.zip" | head -n1 || true)
          LINUX_X64=$(find artifacts -name "*linux-x64*.tar.gz" | head -n1 || true)
          MAC_ARM_DMG=$(find artifacts -name "*macos-arm64*.dmg" | head -n1 || true)
          MAC_ARM_PKG=$(find artifacts -name "*macos-arm64*.pkg" | head -n1 || true)
          MAC_INTEL_DMG=$(find artifacts -name "*macos-intel*.dmg" | head -n1 || true)
          MAC_INTEL_PKG=$(find artifacts -name "*macos-intel*.pkg" | head -n1 || true)

          cat >> RELEASE_NOTES.md <<EOF

          ## 🧩 Downloads

          | <div align="center">Downloads</div> | <div align="center">Compatibility</div> |
          |---|---|
          | [![Windows x64](https://img.shields.io/badge/Windows-x64-0078D6?style=for-the-badge&logo=windows&logoColor=white)](https://github.com/${REPO}/releases/download/${TAG}/chopcord-windows-x64-v${VERSION}.zip) | Windows 10+ |
          | [![macOS ARM64](https://img.shields.io/badge/macOS-ARM64-FFFFFF?style=for-the-badge&logo=apple&logoColor=black)](https://github.com/${REPO}/releases/download/${TAG}/chopcord-macos-arm64-v${VERSION}.dmg) [![PKG ARM64](https://img.shields.io/badge/PKG-ARM64-AAAAAA?style=for-the-badge&logo=apple&logoColor=black)](https://github.com/${REPO}/releases/download/${TAG}/chopcord-macos-arm64-v${VERSION}.pkg) <br/> [![macOS Intel](https://img.shields.io/badge/macOS-Intel-FFFFFF?style=for-the-badge&logo=apple&logoColor=black)](https://github.com/${REPO}/releases/download/${TAG}/chopcord-macos-intel-v${VERSION}.dmg) [![PKG Intel](https://img.shields.io/badge/PKG-Intel-AAAAAA?style=for-the-badge&logo=apple&logoColor=black)](https://github.com/${REPO}/releases/download/${TAG}/chopcord-macos-intel-v${VERSION}.pkg) | macOS 10.15+ |
          | [![Linux x64](https://img.shields.io/badge/Linux-x64-FCC624?style=for-the-badge&logo=linux&logoColor=black)](https://github.com/${REPO}/releases/download/${TAG}/chopcord-linux-x64-v${VERSION}.tar.gz) | GNOME / KDE |

          EOF

          echo "" >> RELEASE_NOTES.md
          echo "## 🔐 Checksums" >> RELEASE_NOTES.md

          for file in \
              "$WIN_X64" \
              "$LINUX_X64" \
              "$MAC_ARM_DMG" "$MAC_ARM_PKG" \
              "$MAC_INTEL_DMG" "$MAC_INTEL_PKG"
          do
            if [ -f "$file" ]; then
              sha=$(sha256sum "$file" | cut -d' ' -f1)
              name=$(basename "$file")
              echo "- **$name:** \`$sha\`" >> RELEASE_NOTES.md
            fi
          done

      - name: Publish GitHub Release
        uses: softprops/action-gh-release@v2
        with:
          tag_name: v${{ github.event.inputs.version }}
          name: Chopcord v${{ github.event.inputs.version }}
          body_path: RELEASE_NOTES.md
          draft: false
          prerelease: true
          files: |
            artifacts/**/*windows*.zip
            artifacts/**/*linux*.tar.gz
            artifacts/**/*macos*.dmg
            artifacts/**/*macos*.pkg
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

